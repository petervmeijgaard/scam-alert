import * as Pagination from './pagination';
import * as PaginationItem from './pagination-item';

export const VPagination = Pagination;
export const VPaginationItem = PaginationItem;

export default {
  VPagination,
  VPaginationItem,
};
