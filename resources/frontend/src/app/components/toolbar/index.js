import * as Toolbar from './toolbar';
import * as ToolbarTitle from './toolbar-title';

export const VToolbar = Toolbar;
export const VToolbarTitle = ToolbarTitle;

export default {
  VToolbar,
  VToolbarTitle,
};
